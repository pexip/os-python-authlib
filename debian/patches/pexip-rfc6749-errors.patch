--- python-authlib.orig/authlib/oauth2/rfc6749/authenticate_client.py
+++ python-authlib/authlib/oauth2/rfc6749/authenticate_client.py
@@ -16,7 +16,7 @@ but the concept of naming are introduced
 
 import logging
 
-from .errors import InvalidClientError
+from .errors import InvalidClientError, UnsupportedAssertionError
 from .util import extract_basic_authorization
 
 log = logging.getLogger(__name__)
@@ -45,12 +45,12 @@ class ClientAuthentication:
                 return client
 
         if "client_secret_basic" in methods:
-            raise InvalidClientError(
+            raise UnsupportedAssertionError(
                 state=request.state,
                 status_code=401,
                 description=f"The client cannot authenticate with methods: {methods}",
             )
-        raise InvalidClientError(
+        raise UnsupportedAssertionError(
             state=request.state,
             description=f"The client cannot authenticate with methods: {methods}",
         )
--- python-authlib.orig/authlib/oauth2/rfc6749/errors.py
+++ python-authlib/authlib/oauth2/rfc6749/errors.py
@@ -110,6 +110,12 @@ class InvalidClientError(OAuth2Error):
         return headers
 
 
+class UnsupportedAssertionError(InvalidClientError):
+    """Client authentication failed due to unsupported
+    authentication method"""
+    description = "unsupported_client_assertion_type"
+
+
 class InvalidGrantError(OAuth2Error):
     """The provided authorization grant (e.g., authorization
     code, resource owner credentials) or refresh token is
